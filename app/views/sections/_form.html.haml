= simple_form_for(@section) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.association :site
    = f.association :section_type
    = f.association :parent_section
    = f.association :user

  .form-actions
    = f.button :submit
