= simple_form_for(@entry_view) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :view_code
    = f.association :entry_type
    = f.association :user

  .form-actions
    = f.button :submit
