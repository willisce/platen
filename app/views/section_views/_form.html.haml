= simple_form_for(@section_view) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :view_code
    = f.association :section_type
    = f.association :user

  .form-actions
    = f.button :submit
